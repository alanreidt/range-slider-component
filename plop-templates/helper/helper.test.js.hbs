import { describe } from 'riteway';

import {{camelCase name}} from './{{camelCase name}}';

describe('{{camelCase name}}', async (assert) => {
  assert({
    given: 'no arguments',
    should: 'return zero',
    actual: {{camelCase name}}(),
    expected: 0,
  });
});
